import { Article, User } from '../types';

const mockAdmin: User = {
  id: 'admin_1722826800000',
  email: 'admin@hbp.com',
  username: 'admin',
  firstName: 'Admin',
  lastName: 'User',
  role: 'admin'
};

export const mockArticles: Article[] = [];

export const mockComments = [];

export default mockAdmin;